{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Lipi main window. Grants the frontend access to a small set of commands; per-phase capabilities are added alongside new commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:window:allow-set-title",
    "core:app:default",
    "updater:default",
    "dialog:default"
  ]
}
