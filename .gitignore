# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Tauri build artifacts
src-tauri/target/
src-tauri/gen/schemas/
src-tauri/tauri-dev.log
src-tauri/lipi-window.png
# screenshot.ps1 is a dev helper; keep it tracked so contributors can use it

# Tauri updater signing keys
# - production/ holds the *real* production keypair. The .pub
#   is committed so the embedded `plugins.updater.pubkey` in
#   tauri.conf.json matches the CI signing key (so end users
#   can verify update signatures). The .key (private) is
#   git-ignored and lives in the CI secret store + an offline
#   backup USB owned by the project lead.
# - dev/ holds a *throwaway* dev keypair. The .pub is committed
#   so contributors can run `tauri build` locally without
#   setting up signing secrets. The .key (private) is
#   git-ignored — never reuse the dev key for any artifact
#   that ships to users.
src-tauri/keys/production/*.key
src-tauri/keys/production/*.key.txt
!src-tauri/keys/production/*.key.pub
src-tauri/keys/dev/*.key
!src-tauri/keys/dev/*.key.pub
# Backwards-compat: any old `lipi-dev.key` at the
# src-tauri/ root is also ignored.
lipi-dev.key
!lipi-dev.key.pub

# Logs
logs/
*.log
verify/*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.cursor/

# OS
Thumbs.db
desktop.ini
ehthumbs.db
$RECYCLE.BIN/

# Env / secrets
.env
.env.local
.env.*.local

# Coverage / cache
coverage/
.cache/
.turbo/
.parcel-cache/

# Lockfile for non-npm package managers (we use npm)
yarn.lock
pnpm-lock.yaml
bun.lockb

# Session debris — verification output files left
# behind by `git add .` after a run of `cargo test`
# / `npm run build` / etc. They are not source.
build-*.txt
build_log.txt
out.txt
verify/
status.txt

# TypeScript incremental build cache
# (`tsc -b` regenerates this on every run; it's
# noise in `git status` and slows down the index.)
tsconfig.tsbuildinfo
